| Question | Human Version | AI Version |
| --- | --- | --- |
| **Easy to understand?** | Yes — simple variables and print statements make it clear. | Yes — uses functions, lists, and dictionaries in a readable way. |
| **Runs successfully?** | Yes — prints two fixed appointments. | Yes — books and displays appointments dynamically. |
| **Uses only required features?** | Yes — basic input/output, lists, dictionaries, and functions. | Yes — stays within beginner‑friendly Python features. |
| **Adds assumptions?** | Yes — assumes correct data and no scheduling conflicts. | Yes — assumes valid input unless validation is added. |
| **Handles errors?** | No — no validation or conflict checking. | No — unless improved, the basic version has no error handling. |
| **Could I explain it?** | Yes — structure is simple and easy to describe. | Yes — logic is clear and beginner‑friendly. |